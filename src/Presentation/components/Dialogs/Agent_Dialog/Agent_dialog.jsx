import { Dialog } from '@mui/material';
import './Agent_Dialog.css'
function AgentDialog({open}){
    return(
        <Dialog open={open}>
            
        </Dialog>
    );
}
export default AgentDialog